name: Styler Checker
on: [push]

jobs:
  linters:
    name: stylelint
    runs-on: Windows-latest

    steps:
      - uses: actions/checkout@v1
      - uses: actions-hub/stylelint@master
        env:
          PATTERN: "*.scss"
          INDENT_SPACES: 4
